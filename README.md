A Lempel-Ziv-Welch compression implementation with a 16 byte fixed-size entry dictionary

### Usage:
compile and run 
./squeeze input.file

if input.file is of the .sqzd format, the command will default to decompression
